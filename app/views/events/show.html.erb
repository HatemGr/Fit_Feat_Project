<h1> <%= @event.title %></h1>

<p>L'evenement se deroule dans la ville de <%= @event.city.name %></p>

<h3>Participtants (<%= @event.users.size + 1 %>) :</h3>
<ul>
  <li> <%= link_to @event.admin.full_name, user_path(@event.admin)%> (admin)</li>
  <%@event.users.each do |user|%>
    <li> <%= link_to user.full_name, user_path(user)%> </li>
  <%end%>
</ul>